import { AllowNumbersOnly } from './allownumbersonly.directive';

describe('AllowNumbersOnly Directive', () => {
  // it('should create an instance', () => {
  //   const directive = new AllowNumbersOnly();
  //   expect(directive).toBeTruthy();
  // });
});
